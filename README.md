# Patch amatoriale per migliorare l’esperienza di gioco

Questa è una patch creata per giocare con i miei amici e migliorare la stabilità online.  
Purtroppo, per politiche interne della società videoludica, i giochi non vengono migliorati ma sostituiti da nuovi titoli.

>  **ATTENZIONE**  
> Contiene testo modificato in modo scherzoso per ridere insieme ai miei amici.  
> In futuro potrebbe arrivare una release più seria.

---

## Se hai **Windows**

### Backup
Avvia `SOLOBACKUP.bat` per creare una lista di file di backup.  
Utile se vuoi disinstallare la patch in seguito.

### Installazione manuale
- Copia il contenuto della cartella `UPDATE` nella cartella del gioco
- Avvia `setpatch.bat` per applicare la patch

### Installazione automatica
- Avvia `installazione.bat`  
- Il file cercherà automaticamente il gioco in **due percorsi predefiniti**  
- Se non lo trova, **modifica lo script** e inserisci il percorso manualmente  
- Non dovrebbe richiedere privilegi di amministratore, ma puoi concederli se vuoi

> Dopo l’installazione **non potrai giocare online con altri utenti**  
> Se vuoi tornare alla versione compatibile con il multiplayer:
> - Avvia `disinstallazione.bat`  (è in beta e potrebbe non funzionare)
> - Funziona solo se hai eseguito `installazione.bat` o `SOLOBACKUP.bat`  
> - Usa i file di backup per ripristinare la versione originale

---

## Se hai **macOS**

La versione per Mac è ancora in fase di sviluppo.  
🕐 Stay tuned!

---

## Modifiche al gameplay

Consulta il file `Log.txt` per conoscere tutte le modifiche apportate.

---
