--collegare eventi fra lua
--Puoi fare un evento in un lua
function OnExitGame()
    --UIManager:QueuePopup( ContextPtr, PopupPriority.eUtmost );
	--UIManager:PushModal( ContextPtr );
	UI.ExitGame();
end
Events.UserRequestClose.Add( OnExitGame );
--crea un evento che chiama l'evento sopra
function OnExitGame()
	Events.UserRequestClose();
end
Controls.ExitButton:RegisterCallback( Mouse.eLClick, OnExitGame );


Per colorare testi [COLOR_POSITIVE_TEXT] [ENDCOLOR] [COLOR_NEGATIVE_TEXT]